---
- hosts: "{{hosts}}"
  tasks:
  - name: template file
    template: src=/data/web/testplaybook/file/{{ item }} dest={{ProjectPath}}/config/{{ item }}
    with_items:
      - config_1.ini
      - config_2.ini
      - config_3.ini
      - config_4.ini
      - config_5.ini
...
